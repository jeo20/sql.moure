-- SELECT realiza consultas en una tabla

-- devuelve todas las columnas de la tabla users
SELECT * FROM users;

-- devuelve solamente la columna nombre
SELECT name FROM users;

-- devuelve el user_id y el name
SELECT user_id, name FROM users;

